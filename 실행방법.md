# 로컬 개발 서버 실행 방법

## 최초 1회 — Node.js PATH 설정

Node.js가 `AppData\Local\nodejs`에 설치되어 있어 터미널을 새로 열 때마다 PATH를 잡아줘야 합니다.

**PowerShell 기준:**
```powershell
$env:Path = "$env:LOCALAPPDATA\nodejs;" + $env:Path
```

CMD는 지원 안 됨. PowerShell 또는 VS Code 터미널 사용 권장.

---

## 개발 서버 실행

```powershell
$env:Path = "$env:LOCALAPPDATA\nodejs;" + $env:Path
cd "c:\Users\JUNGFRAU\Desktop\여행"
npm run dev
```

브라우저에서 http://localhost:5173/travel-app/ 접속.

`Ctrl+C`로 서버 종료.

---

## 빌드 (배포용)

```powershell
$env:Path = "$env:LOCALAPPDATA\nodejs;" + $env:Path
cd "c:\Users\JUNGFRAU\Desktop\여행"
npm run build
```

`dist/` 폴더에 결과물 생성.

---

## Firebase 보안 규칙 배포

코드 변경 시 `firestore.rules` 내용을 Firebase Console에 붙여넣고 게시해야 반영됩니다.

https://console.firebase.google.com/project/vacation-project-001/firestore/rules

---

## 환경변수

`.env.local` 파일에 키가 설정되어 있어야 Firebase 연결이 됩니다.
없으면 `.env.local.example`을 복사해서 값을 채워주세요.

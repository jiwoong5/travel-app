# Architecture Decision Records

---

## ADR-001 프론트엔드 아키텍처 패턴

**날짜**: 2026-06-20  
**상태**: 확정

### 결정
단방향 데이터 흐름 (Unidirectional Data Flow) 패턴 채택

### 배경
백엔드를 Firebase에 완전히 위임하는 서버리스 구조로, Firebase `onSnapshot`이 서버에서 클라이언트로 능동적으로 데이터를 푸시한다. 데이터 변경의 트리거가 사용자 행동이 아닌 외부 푸시인 경우가 많다.

### 검토한 대안
- **단순 모듈 패턴**: 페이지 간 공유 상태(현재 그룹 등)를 관리할 위치가 애매하고, 리스너가 페이지마다 중복될 수 있음
- **MVC**: 사용자 요청 → 서버 응답 흐름을 전제로 한 패턴이라 Firebase 외부 푸시를 자연스럽게 수용하기 어려움
- **단방향 데이터 흐름**: 데이터 출처에 관계없이 항상 동일한 경로로 처리. Firebase 공식 연동 라이브러리(ReactFire, VueFire, AngularFire)가 모두 이 패턴을 따름

### 결정 이유
Firebase 아키텍처가 단방향 데이터 흐름을 전제로 설계되어 있으므로, Firebase가 권장하는 방향을 따르는 것이 자연스럽다.

---

## ADR-002 프론트엔드 프레임워크

**날짜**: 2026-06-20  
**상태**: 확정

### 결정
Svelte 채택

### 배경
ADR-001에서 단방향 데이터 흐름 패턴을 채택했고, Firebase `onSnapshot`과 자연스럽게 연동되는 반응형 프레임워크가 필요하다.

### 검토한 대안

| | Svelte | Vue 3 | React |
|--|--------|-------|-------|
| 반응형 방식 | 언어 내장 (컴파일 타임) | 런타임 반응형 시스템 | 런타임 Virtual DOM |
| 번들 크기 | 가장 작음 (런타임 없음) | 중간 | 가장 큼 |
| Firebase 연동 | SDK 직접 사용 | VueFire (추가 의존성) | ReactFire (추가 의존성) |
| 코드량 | 가장 적음 | 중간 | 가장 많음 |

### 결정 이유
- 반응형이 컴파일 타임에 처리되어 런타임 오버헤드가 없고, 컴파일 결과가 Vanilla JS라 GitHub Pages 정적 배포에 적합하다
- Firebase SDK와 추가 연동 라이브러리 없이 직접 연결되어 의존성이 단순하다
- 코드량이 가장 적어 이 프로젝트의 단순한 스타일 방침과 일치한다

---

## ADR-003 클라이언트 라우팅 방식

**날짜**: 2026-06-20  
**상태**: 확정

### 결정
Hash 라우팅 채택 (`/#/votes` 형태), 별도 라이브러리 없이 구현

### 배경
GitHub Pages는 정적 파일 서버라 `/votes` 같은 경로로 직접 접근하면 서버가 해당 파일을 찾지 못해 404가 발생한다.

### 검토한 대안
- **Hash 라우팅**: URL의 `#` 이후는 브라우저가 처리하므로 서버 요청이 발생하지 않음. 추가 라이브러리 불필요
- **svelte-routing**: `/votes` 형태의 깔끔한 URL 가능하나, GitHub Pages에서 404 fallback 설정 필요 (404.html 트릭 사용). 라이브러리 의존성 추가

### 결정 이유
이 프로젝트 규모에서 깔끔한 URL의 이점보다 설정 복잡도가 더 크다. Hash 라우팅이 GitHub Pages와 즉시 호환되고 의존성도 없어 단순함을 유지할 수 있다.

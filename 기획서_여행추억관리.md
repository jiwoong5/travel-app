# 기획서 — 여행 추억 관리

## 기능 개요

여행을 다녀온 후 그룹원들이 사진과 메모를 함께 올리고, 공동 앨범처럼 열람하는 기능.

---

## 주요 화면

### 1. 추억 목록 화면
- 그룹 내 여행별 추억 목록 (여행명, 날짜, 사진 수 표시)
- [새 추억 앨범 만들기] 버튼

### 2. 추억 앨범 생성 화면
- 앨범 제목 (예: "제주 여행 2025 여름")
- 연결된 여행 일정 선택 (선택 사항)
- [만들기] 버튼

### 3. 앨범 상세 화면
- 앨범이 여행 일정과 연결된 경우: 사진을 장소별로 그룹핑하여 표시
  - 일정 항목과 연결된 사진: 해당 장소 섹션 아래에 표시
  - 연결되지 않은 사진: "기타" 섹션에 표시
- 앨범이 일정과 연결되지 않은 경우: 업로드 순으로 단순 목록
- 각 사진 카드에 표시: 장소명 (연결된 경우) + 한 줄 코멘트
- [사진 추가] 버튼
- 사진 클릭 시 상세 보기

### 4. 사진 업로드 화면
- 파일 선택 (이미지 파일)
- 일정 항목 연결 (선택):
  - 앨범이 여행 일정과 연결된 경우에만 표시
  - 해당 여행의 일정 항목을 날짜별로 드롭다운 표시
  - 선택 시 `scheduleItemId`, `placeName`, `date` 자동 저장
- 한 줄 코멘트 입력 (선택): 사진에 담긴 상황이나 특이한 순간 기록
- [업로드] 버튼

### 5. 사진 상세 화면
- 사진 원본 표시
- 장소명 표시 (일정 항목 연결된 경우)
- 한 줄 코멘트 표시
- [코멘트 추가] 기능 (다른 그룹원들도 코멘트 가능)
- [삭제] 버튼 (업로더 또는 그룹 관리자만)

---

## 데이터 모델

```
MemoryAlbum (추억 앨범)
  - id
  - group_id
  - trip_id (nullable, 일정과 연결 시)
  - title
  - created_by
  - created_at

Photo (사진)
  - id
  - album_id
  - file_path (서버 저장 경로)
  - original_filename
  - uploaded_by
  - uploaded_at
  - comment (한 줄 코멘트 — 사진 속 상황이나 특이한 순간)
  - schedule_item_id (nullable) ← 연결된 일정 항목 ID
  - place_name (nullable)       ← 일정 항목의 장소명 (비정규화 저장)
  - date (nullable)             ← 일정 항목의 날짜 (장소별 그룹핑용)

PhotoComment (사진 코멘트)
  - id
  - photo_id
  - user_id
  - content
  - created_at
```

---

## 비즈니스 규칙

- 사진 업로드: 그룹원 누구나 가능
- 사진 삭제: 업로더 본인만 가능 (또는 그룹장)
- 파일 형식: jpg, jpeg, png, gif, webp만 허용
- 파일 크기 제한: 업로드 시 10MB 이하 (서버에서 검증)
- 코멘트: 그룹원 누구나 작성 가능, 본인 코멘트만 삭제 가능

---

## 엣지 케이스

- 이미지 아닌 파일 업로드 시도: 오류 메시지 표시, 업로드 차단
- 앨범에 사진이 없는 상태: "아직 추가된 사진이 없습니다" 문구
- 대용량 파일: 업로드 전 클라이언트 측에서도 사전 차단
- 삭제된 사진에 달린 코멘트: 사진 삭제 시 코멘트도 함께 삭제

---

## UI 메모

- 사진 그리드: 단순 테이블 또는 flex 기반 3열 배열
- 썸네일은 고정 크기로 자르기 (object-fit: cover)
- 확대 보기: 별도 페이지로 이동 (모달 생략)
- 정렬: 기본값은 업로드일 내림차순
- 다운로드 버튼: 원본 파일 다운로드 링크 제공

# 기획서 — 투표 (여행지 투표 / 관광지 투표)

## 기능 개요

투표 기능은 두 단계로 구성된다.

1. **여행지 투표**: 어디로 갈지 후보지를 등록하고 그룹원이 투표하여 여행지 확정
2. **관광지 투표**: 여행지 확정 후 해당 지역에서 방문할 관광지를 투표로 결정

두 투표는 동일한 구조(데이터 모델, 화면)를 공유하며, `vote_type` 필드로 구분한다.

---

## 투표 상태 흐름

```
[투표 생성] → recruiting(후보 모집 중) → [투표 시작] → ongoing(투표 중) → [마감] → closed(마감)
```

## 주요 화면

### 1. 투표 목록 화면
- 현재 그룹의 투표 목록 표시
- 각 항목에 상태 배지 표시: [모집중] / [투표중] / [마감]
- 투표 유형 배지: [여행지] / [관광지]
- [새 투표 만들기] 버튼

### 2. 투표 생성 화면
- 투표 유형 선택: 여행지 투표 / 관광지 투표
  - 관광지 투표 선택 시: 마감된 여행지 투표의 당선 여행지를 드롭다운으로 표시하여 선택
- 투표 제목 입력 (예: "여름 여행지 투표", "제주 관광지 투표")
- 마감 날짜 설정 (선택)
- [모집 시작] 버튼 → status: recruiting으로 생성 (후보 없이)

### 3. 후보 모집 화면 (recruiting)
- 후보 목록 표시 (등록자 표시)
- [후보 추가] 폼: 장소명, 설명, Google Maps URL
- 본인이 등록한 후보에만 [삭제] 버튼 표시
- 생성자에게 [투표 시작] 버튼 표시 (후보 1개 이상일 때만 활성화)

### 4. 투표 참여 화면 (ongoing)
- 후보 목록 (이름, 설명, 현재 득표수)
- Google Maps URL이 있는 항목: 인라인 지도 미리보기
- 각 항목 옆에 [투표] 버튼
- 마감 전: 투표 가능 / 마감 후: 결과만 열람
- 생성자에게 [마감] 버튼 표시

### 5. 투표 결과 화면 (closed)
- 득표 순서대로 후보 정렬
- 1위 항목에 "당선" 표시 (동률 시 공동 당선 모두 표시)
- 여행지 투표 마감 시 당선 여행지명을 `winners` 배열로 DB에 저장
- 관광지 투표 결과는 일정 화면에서 드롭다운으로 직접 활용 (별도 연결 버튼 없음)

---

## 데이터 모델

```
Vote (투표)
  - id
  - group_id
  - title
  - vote_type (destination / attraction)   ← 여행지 투표 / 관광지 투표 구분
  - linked_destination (nullable)           ← 관광지 투표 시 연결된 당선 여행지명
  - winners (object[], nullable)            ← 마감 시 저장, 동률 시 복수
                                              { placeName, description, mapUrl }
  - deadline
  - status (recruiting / ongoing / closed)  ← 모집중 / 투표중 / 마감
  - created_by
  - created_at

VoteOption (후보 항목)
  - id
  - vote_id
  - place_name
  - description
  - map_url (optional)      ← 사용자가 붙여넣은 Google Maps 공유 URL
  - place_id (optional)     ← Places API 연계 시 추출한 Google Place ID
  - created_by              ← 후보 등록자 (모집 단계 삭제 권한 판단에 사용)

VoteRecord (투표 기록)
  - id
  - vote_id
  - option_id
  - user_id
  - voted_at
```

---

## 비즈니스 규칙

- 후보 등록: recruiting 상태에서 그룹원 누구나 가능
- 후보 삭제: recruiting 상태에서 본인이 등록한 후보만 삭제 가능 (생성자도 남의 후보 삭제 불가)
- 투표 삭제: 생성자만 가능 (투표 전체 삭제)
- 투표 시작: 생성자만 가능, 후보 1개 이상일 때만 활성화
- 후보 추가/삭제: ongoing, closed 상태에서 불가
- 1인 1투표 (같은 투표에서 중복 불가)
- 투표 마감 후 결과 변경 불가
- 관광지 투표는 여행지 투표가 closed된 이후에만 생성 가능

---

## 엣지 케이스

- 동률 발생 시: 공동 당선 항목 모두 `winners`에 저장 → 관광지 투표 생성 드롭다운에 모두 노출
- 후보지 0개인 상태로 투표 시작 불가
- 마감일이 지났는데 아무도 투표 안 한 경우: "투표 없이 마감" 표시

---

## Google Maps 연계

- 사용자가 Google Maps 공유 URL을 직접 붙여넣음
- `map_url`에서 장소 정보를 추출하여 `<iframe>` 임베드
- 후보 항목 카드 하단에 소형 지도 표시 (클릭 없이 바로 위치 확인 가능)
- API 키 필요, 월 28,500회 무료 (소규모 서비스 기준 사실상 무료)
- 구현 방식: `https://www.google.com/maps/embed/v1/place?key=API_KEY&q=place_id:PLACE_ID`

---

## 엣지 케이스 (지도 관련 추가)

- 유효하지 않은 Google Maps URL 입력 시: 임베드 오류 → [지도 보기] 링크로 fallback
- map_url 없는 항목: 지도 영역 미표시 (레이아웃 무너지지 않게 처리)
- 단축 URL (`maps.app.goo.gl/...`) 입력 시: 서버에서 리다이렉트 따라가 원본 URL 추출 필요

---

## UI 메모

- 결과는 숫자(득표수)만 표시 (막대 그래프 등 생략)
- 투표 버튼은 단순 텍스트 버튼
- 자신이 투표한 항목에 "(내 선택)" 표시
- 지도 임베드는 후보 카드 하단에 고정 높이(예: 200px)로 삽입
- 지도 없는 항목과 있는 항목이 섞여도 카드 높이가 통일되도록 처리

# 기획서 — 여행 일정 관리

## 기능 개요

그룹원들이 여행 날짜별 일정을 공동으로 작성하고 실시간 공유하는 기능.  
여행지 투표에서 확정된 장소를 기반으로 일정을 구성할 수 있음.

---

## 주요 화면

### 1. 일정 목록 화면
- 그룹 내 생성된 여행 일정 목록
- 여행명, 기간, 장소 요약 표시
- [새 일정 만들기] 버튼

### 2. 일정 생성 화면
- 여행명 입력 (예: "제주 3박 4일")
- 여행 시작일 / 종료일 선택
- 여행지 입력:
  - 마감된 여행지 투표의 당선 여행지가 있으면 드롭다운으로 표시
  - 없으면 텍스트 직접 입력
- [일정 만들기] 버튼

### 3. 일정 상세 화면 (날짜별 섹션)
- 날짜별 섹션으로 구분 (Day 1, Day 2, ...)
- 각 날짜에 여러 개의 일정 항목 추가 가능

#### 투표 연동 패널
- 일정 상세 상단에 고정 표시
- 해당 여행지와 연결된 마감된 관광지 투표의 당선 관광지 목록을 드롭다운으로 표시
- 선택 시 일정 항목 추가 폼에 장소명 자동 입력

#### 일정 항목(카드) 구성
- 시간 (선택)
- 장소명
- 카테고리: 이동 / 식사 / 관광 / 숙박 / 기타
- 메모 (자유 텍스트)
- Google Maps 링크 (선택, 지도 임베드)
- 작성자 표시
- [수정] [삭제] 버튼

### 4. 일정 항목 추가/수정 화면
- 시간 입력 (선택)
- 장소명 입력 (투표 연동 패널에서 선택 시 자동 입력)
- 카테고리 선택
- 메모 입력 (투표 연동 선택 시 후보 설명 자동 입력)
- Google Maps URL (투표 연동 선택 시 자동 입력)
- [저장] 버튼

---

## 데이터 모델

```
Trip (여행 일정)
  - id
  - group_id
  - title
  - destination
  - start_date
  - end_date
  - created_by
  - created_at

ScheduleItem (일정 항목)
  - id
  - trip_id
  - date
  - time (nullable)
  - place_name
  - category (move / meal / sightseeing / accommodation / etc)
  - memo
  - map_url (nullable)   ← 투표 연동 시 자동 입력 또는 직접 입력
  - created_by
  - created_at
  - updated_at
```

---

## 비즈니스 규칙

- 일정 항목은 그룹원 누구나 추가/수정/삭제 가능
- 삭제 시 "누가 삭제했는지" 기록 (복잡한 권한 관리 없이 단순 로그)
- 날짜 범위는 여행 시작일~종료일 내로 제한
- 같은 날짜에 항목 수 제한 없음

---

## 엣지 케이스

- 여행 기간이 수정되면 범위 밖 일정 항목 처리: 경고 후 삭제 또는 유지 선택
- 항목이 없는 날짜는 "아직 일정 없음" 문구 표시
- 동시 수정 충돌: 마지막 저장 기준으로 덮어쓰기 (낙관적 잠금 생략)

---

## UI 메모

- 날짜별 섹션은 단순 구분선으로 구분
- 시간 입력이 있는 항목은 시간순 정렬, 없는 항목은 하단에 배치
- 카테고리는 텍스트 셀렉트박스 (아이콘 없음)
- 인쇄 가능한 형태로 출력 기능 고려 (CSS print 미디어쿼리)
